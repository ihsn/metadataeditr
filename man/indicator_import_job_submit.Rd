% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator.R
\name{indicator_import_job_submit}
\alias{indicator_import_job_submit}
\title{Submit an indicator data import job}
\usage{
indicator_import_job_submit(
  project_id,
  upload_id,
  indicator_value,
  delimiter = ",",
  priority = 0,
  api_key = NULL,
  api_base_url = NULL
)
}
\arguments{
\item{project_id}{(required) Numeric project id or project idno (same as other editor APIs)}

\item{upload_id}{(required) Completed upload session ID from \code{upload_file_chunked}}

\item{indicator_value}{(required) The indicator code to import (e.g. \code{"NY.GDP.PCAP.CD"}).
Only rows whose indicator_id column matches this value will be imported.}

\item{delimiter}{CSV field delimiter character. Default \code{","}}

\item{priority}{Job queue priority. Default 0}

\item{api_key}{API key (optional if set via set_api_key)}

\item{api_base_url}{API base endpoint (optional if set via set_api_url)}
}
\value{
List with \code{status_code}, \code{uuid} and \code{response}
}
\description{
Submits a background job (\code{POST /api/jobs/import_indicator_data}) that
loads a pre-uploaded CSV into the timeseries data store for an indicator or
timeseries project.  The CSV must already be uploaded via
\code{\link{upload_file_chunked}} before calling this function.
}
\details{
Use \code{\link{import_indicator_data}} for a single call that handles upload,
job submission, and polling in one step.
}
