% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator.R
\name{job_wait}
\alias{job_wait}
\title{Poll a background job until it completes}
\usage{
job_wait(
  job_uuid,
  timeout_seconds = 1800,
  interval_seconds = 5,
  show_progress = TRUE,
  api_key = NULL,
  api_base_url = NULL
)
}
\arguments{
\item{job_uuid}{(required) Job UUID returned by a job-creation endpoint}

\item{timeout_seconds}{Maximum time to wait in seconds. Default 1800 (30 min)}

\item{interval_seconds}{Seconds between polling attempts. Default 5}

\item{show_progress}{Print status messages to the console. Default TRUE}

\item{api_key}{API key (optional if set via set_api_key)}

\item{api_base_url}{API base endpoint (optional if set via set_api_url)}
}
\value{
List with \code{status_code} and \code{response} (the final job object)
}
\description{
Repeatedly calls \code{GET /api/jobs/{uuid}} until the job reaches a terminal
state (\code{completed} or \code{failed}), the timeout is reached, or an HTTP
error occurs.
}
