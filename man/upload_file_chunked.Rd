% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upload.R
\name{upload_file_chunked}
\alias{upload_file_chunked}
\title{Upload a file using chunked resumable upload}
\usage{
upload_file_chunked(
  file_path,
  chunk_size = 10485760,
  metadata = list(),
  resume_upload_id = NULL,
  show_progress = TRUE,
  api_key = NULL,
  api_base_url = NULL
)
}
\arguments{
\item{file_path}{(required) Path to the local file to upload}

\item{chunk_size}{Chunk size in bytes. Defaults to 10 MB (10485760)}

\item{metadata}{Optional named list of metadata to store with the upload session}

\item{resume_upload_id}{Upload session ID to resume a previous upload. If NULL a new
session is initialized automatically.}

\item{show_progress}{Print progress to the console (TRUE/FALSE). Default TRUE.}

\item{api_key}{API key (optional if set via set_api_key)}

\item{api_base_url}{API base endpoint (optional if set via set_api_url)}
}
\value{
List with:
  \itemize{
    \item \code{status_code} - HTTP status of the final chunk response
    \item \code{upload_id}   - The upload session ID
    \item \code{response}    - Full response body from the last chunk POST
  }
}
\description{
Splits a local file into chunks and uploads them to the server one at a time.
Already-uploaded chunks are skipped automatically, making the upload resumable
if interrupted.
}
\details{
The chunk size defaults to 10 MB. You can override it or call \code{upload_limits()}
first to fetch the server's recommended chunk size.
}
\examples{

# Basic usage
result <- upload_file_chunked(file_path = "/path/to/large-file.zip")

# Use a custom 5 MB chunk size
result <- upload_file_chunked(
  file_path  = "/path/to/large-file.zip",
  chunk_size = 5242880
)

# Resume an interrupted upload
result <- upload_file_chunked(
  file_path        = "/path/to/large-file.zip",
  resume_upload_id = "abc123-upload-id"
)

}
